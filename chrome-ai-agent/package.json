{
  "name": "chrome-ai-agent",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm --prefix server test && npm run test:e2e",
    "check": "npm --prefix server run check && node --check extension/background.js && node --check extension/content.js && node --check extension/sidepanel.js",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
